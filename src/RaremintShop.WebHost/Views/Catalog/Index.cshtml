@model List<RaremintShop.Module.Catalog.Models.CatalogViewModel>

@{
    ViewBag.Title = "商品一覧";
}

<div class="container mt-5">
    <h1 class="text-center mb-5">商品一覧</h1>

    @* <div class="row justify-content-center">
        <!-- 商品リストをループして表示 -->
        @foreach (var product in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm">
                    <!-- 商品画像を表示。複数の画像がある場合は最初の画像を使用 -->
                    <img src="@product.ImageUrls.FirstOrDefault()" class="card-img-top" alt="@product.Name">
                    <div class="card-body text-center">
                        <!-- 商品名を表示 -->
                        <h5 class="card-title">@product.Name</h5>
                        <!-- 商品説明を表示 -->
                        <p class="card-text">@product.Description</p>
                        <!-- 商品価格を表示。通貨形式にフォーマット -->
                        <p class="card-text font-weight-bold">@product.Price.ToString("C")</p>
                        <!-- 詳細ページへのリンクボタン -->
                        <a href="#" class="btn btn-primary">詳細を見る</a>
                    </div>
                </div>
            </div>
        }
    </div> *@
</div>